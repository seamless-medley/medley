{
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "build:server": {
      "outputs": ["dist/**"]
    },
    "combine-discord": {
      "dependsOn": ["build:server"],
      "outputs": ["combine/discord/**"]
    }
  }
}
