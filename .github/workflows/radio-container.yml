name: Build, package and publish to ghcr.io

on:
  push:
    tags:
      - 'radio@v*'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  HUSKY: 0

jobs:
  extract-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.package-version.outputs.version }}
      major: ${{ steps.package-version.outputs.major }}
      major_minor: ${{ steps.package-version.outputs.major_minor }}
      is_prerelease: ${{ steps.package-version.outputs.is_prerelease }}
      prerelease_tag: ${{ steps.package-version.outputs.prerelease_tag }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            packages/radio/package.json
          sparse-checkout-cone-mode: false

      - name: Extract version from package.json
        id: package-version
        run: |
          VERSION=$(node -p "require('./packages/radio/package.json').version")
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

          if [[ "$VERSION" =~ - ]]; then
            echo "is_prerelease=true" >> $GITHUB_OUTPUT
            PRERELEASE_TAG=$(echo "$VERSION" | sed -E 's/.*-([a-z]+).*/\1/')
            echo "prerelease_tag=${PRERELEASE_TAG}" >> $GITHUB_OUTPUT
          else
            echo "is_prerelease=false" >> $GITHUB_OUTPUT
            echo "prerelease_tag=" >> $GITHUB_OUTPUT

            MAJOR="${VERSION%%.*}"
            MINOR="${VERSION#*.}"
            MINOR="${MINOR%%.*}"
            echo "major=${MAJOR}" >> $GITHUB_OUTPUT
            echo "major_minor=${MAJOR}.${MINOR}" >> $GITHUB_OUTPUT
          fi

  build-image:
    needs: extract-version
    strategy:
      matrix:
        include:
          - runs-on: ubuntu-latest
            arch: amd64
            platforms: linux/amd64

          - runs-on: [self-hosted, macOS, ARM64, docker]
            arch: arm64
            platforms: linux/arm64

    name: Build ${{ matrix.arch }} image
    runs-on: ${{ matrix.runs-on }}
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        id: checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ needs.extract-version.outputs.version }},suffix=-${{ matrix.arch }}
            type=raw,value=${{ needs.extract-version.outputs.major_minor }},suffix=-${{ matrix.arch }},enable=${{ needs.extract-version.outputs.is_prerelease == 'false' }}
            type=raw,value=${{ needs.extract-version.outputs.major }},suffix=-${{ matrix.arch }},enable=${{ needs.extract-version.outputs.is_prerelease == 'false' }}
            type=raw,value=${{ needs.extract-version.outputs.prerelease_tag }},suffix=-${{ matrix.arch }},enable=${{ needs.extract-version.outputs.is_prerelease == 'true' }}
            type=raw,value=latest,suffix=-${{ matrix.arch }},enable=${{ needs.extract-version.outputs.is_prerelease == 'false' }}
            type=sha,suffix=-${{ matrix.arch }}

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./packages/radio/docker/Dockerfile
          build-args: |
            ARCH=${{ matrix.arch }}
          platforms: ${{ matrix.platforms }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          push: true

  push-images:
    needs:
      - extract-version
      - build-image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create and push manifest images
        uses: Noelware/docker-manifest-action@v1
        with:
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.extract-version.outputs.version }}
          inputs: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.extract-version.outputs.version }}-amd64,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.extract-version.outputs.version }}-arm64
          push: true

      - name: Create and push manifest images (prerelease tag)
        if: needs.extract-version.outputs.is_prerelease == 'true'
        uses: Noelware/docker-manifest-action@v1
        with:
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.extract-version.outputs.prerelease_tag }}
          inputs: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.extract-version.outputs.prerelease_tag }}-amd64,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.extract-version.outputs.prerelease_tag }}-arm64
          push: true

      - name: Create and push manifest images (major.minor)
        if: needs.extract-version.outputs.is_prerelease == 'false'
        uses: Noelware/docker-manifest-action@v1
        with:
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.extract-version.outputs.major_minor }}
          inputs: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.extract-version.outputs.major_minor }}-amd64,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.extract-version.outputs.major_minor }}-arm64
          push: true

      - name: Create and push manifest images (major)
        if: needs.extract-version.outputs.is_prerelease == 'false'
        uses: Noelware/docker-manifest-action@v1
        with:
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.extract-version.outputs.major }}
          inputs: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.extract-version.outputs.major }}-amd64,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.extract-version.outputs.major }}-arm64
          push: true

      - name: Create and push manifest images (latest)
        if: needs.extract-version.outputs.is_prerelease == 'false'
        uses: Noelware/docker-manifest-action@v1
        with:
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          inputs: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-amd64,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-arm64
          push: true
